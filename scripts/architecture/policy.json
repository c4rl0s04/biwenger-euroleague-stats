{
  "entrypoints": [
    "src/app/(app)/dashboard/page.tsx",
    "src/app/(app)/dashboard/[section]/page.tsx",
    "src/app/api/dashboard/birthdays/route.ts",
    "src/app/api/dashboard/rising-stars/route.ts",
    "src/app/api/dashboard/top-players/route.ts",
    "src/app/api/dashboard/top-form/route.ts",
    "src/app/api/dashboard/market-opportunities/route.ts",
    "src/app/api/dashboard/mvps/route.ts",
    "src/app/api/dashboard/next-round/route.ts",
    "src/app/api/dashboard/leader-gap/route.ts",
    "src/app/api/dashboard/recent-activity/route.ts",
    "src/app/api/dashboard/ideal-lineup/route.ts",
    "src/app/api/league-average/route.ts",
    "src/app/(app)/market/[section]/page.tsx",
    "src/app/(app)/market/page.tsx",
    "src/app/api/market/stats/route.ts",
    "src/app/api/market/route.ts",
    "src/app/api/market/transfers/route.ts",
    "src/app/api/market/stats/value-details/route.ts",
    "src/app/api/market/duels/details/route.ts",
    "src/app/api/market/trends/route.ts",
    "src/app/(app)/playoffs/page.tsx",
    "src/app/(app)/playoffs/predictions/[userId]/page.tsx",
    "src/app/(app)/predictions/page.tsx",
    "src/app/(app)/predictions/[section]/page.tsx",
    "src/app/(app)/tournaments/page.tsx",
    "src/app/(app)/tournaments/[id]/page.tsx",
    "src/app/(app)/tournaments/[id]/[section]/page.tsx",
    "src/app/(app)/user/[id]/page.tsx",
    "src/app/(app)/user/[id]/[section]/page.tsx",
    "src/app/(app)/matches/page.tsx",
    "src/app/(app)/matches/round/[roundId]/page.tsx",
    "src/app/(app)/team/[id]/page.tsx",
    "src/app/(app)/team/[id]/[section]/page.tsx",
    "src/app/api/team/[id]/route.ts",
    "src/app/api/matches/[id]/shots/route.ts",
    "src/app/api/matches/[id]/play-by-play/route.ts",
    "src/app/api/player/rounds/route.ts",
    "src/app/api/player/stats/route.ts",
    "src/app/api/player/squad/route.ts",
    "src/app/(app)/standings/page.js",
    "src/app/api/standings/full/route.ts",
    "src/app/api/standings/league-totals/route.ts",
    "src/app/api/standings/value-ranking/route.ts",
    "src/app/api/search/route.ts",
    "src/app/(app)/rounds/page.tsx",
    "src/app/(app)/rounds/[roundId]/[section]/page.tsx",
    "src/app/api/rounds/all-history/route.ts",
    "src/app/api/rounds/history/route.ts",
    "src/app/api/rounds/leaderboard/route.ts",
    "src/app/api/rounds/lineup-stats/route.ts",
    "src/app/api/rounds/lineup/route.ts",
    "src/app/api/rounds/list/route.ts",
    "src/app/api/rounds/standings/route.ts",
    "src/app/api/rounds/stats/route.ts",
    "src/app/(app)/standings/[section]/page.tsx",
    "src/app/api/standings/advanced/route.ts",
    "src/app/api/standings/analytics/route.ts",
    "src/app/api/standings/bottlers/route.ts",
    "src/app/api/standings/captains/route.ts",
    "src/app/api/standings/efficiency/route.ts",
    "src/app/api/standings/heartbreakers/route.ts",
    "src/app/api/standings/initial-squad-stats/route.ts",
    "src/app/api/standings/jinx/route.ts",
    "src/app/api/standings/league-comparison/route.ts",
    "src/app/api/standings/no-glory/route.ts",
    "src/app/api/standings/placements/route.ts",
    "src/app/api/standings/points-progression/route.ts",
    "src/app/api/standings/round-winners/route.ts",
    "src/app/api/standings/streaks/route.ts",
    "src/app/api/standings/theoretical/route.ts",
    "src/app/api/standings/volatility/route.ts",
    "src/app/api/users/route.ts",
    "src/app/api/dashboard/captain-stats/route.ts",
    "src/app/api/dashboard/home-away/route.ts",
    "src/app/api/dashboard/captain-suggest/route.ts",
    "src/app/(app)/schedule/page.tsx",
    "src/app/(app)/schedule/map/page.tsx",
    "src/app/(app)/compare/page.tsx",
    "src/app/(app)/compare/[userId]/page.tsx",
    "src/app/api/compare/data/route.ts",
    "src/app/api/compare/data/lite/route.ts"
  ],
  "exceptions": [
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/[section]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Dashboard page authentication reaches frozen authentication/credential infrastructure. Presentation migration preserves that security boundary; this does not exempt Dashboard domain queries.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint: src/app/(app)/dashboard/page.tsx -> src/lib/services/app/news-landing-legacy.ts",
      "reason": "Phone overview retains the existing shared News feed; Task 07 moves Dashboard presentation, not News reads.",
      "removeWhen": "Task 08 provides the News server contract and updates this page."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/dashboard/page.tsx -> src/lib/services/app/news-landing-legacy.ts -> src/lib/db/index.ts",
      "reason": "The existing News feed still reaches its legacy database barrel. This exact temporary edge is outside Dashboard-owned data orchestration.",
      "removeWhen": "Task 08 migrates News queries/services and this page consumes its server contract."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/leader-gap/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/rounds/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/stats/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/player/squad/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/rounds/[roundId]/[section]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Rounds page session identity reaches frozen authentication/credential infrastructure, not Rounds statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-stats/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/home-away/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/auth.js -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/api/dashboard/captain-suggest/route.ts -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing getRequestUserId session fallback reaches frozen authentication/credential infrastructure, not manager statistical queries. This exact edge preserves the current security boundary without authorizing other persistence helpers.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/schedule/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Schedule page session identity reaches frozen authentication/credential infrastructure, not Schedule statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/auth.js -> drizzle-orm",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/auth.js -> src/lib/db/index.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/auth.js -> src/lib/db/schema.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/lib/credentials/repository.ts -> drizzle-orm",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/index.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    },
    {
      "edge": "entrypoint-persistence: src/app/(app)/compare/[userId]/page.tsx -> src/lib/credentials/repository.ts -> src/lib/db/schema.ts",
      "reason": "Existing Compare page session identity reaches frozen authentication/credential infrastructure, not Compare statistical queries. Preserve this exact pre-existing access flow until the separate Accounts security gate.",
      "removeWhen": "Accounts/credential infrastructure passes its separate security gate and exposes a deliberate authentication contract recognized by the architecture policy."
    }
  ]
}
