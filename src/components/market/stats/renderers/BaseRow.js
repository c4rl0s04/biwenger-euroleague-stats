'use client';

/**
 * Market-specific BaseRow re-export.
 * This ensures compatibility with existing market renderers while centralizing
 * the visual template in the global UI library.
 */
export { default } from '@/components/ui/BaseRow';
