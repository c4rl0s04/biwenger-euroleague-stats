export interface HighestTransferRecord {
  precio: number | string | null;
  player_name: string | null;
  comprador: string | null;
}
export interface BiggestGainRecord {
  name: string | null;
  price_increment: number | string;
}
