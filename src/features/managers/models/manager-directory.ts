/** Public fantasy directory projection; not account/session data. */
export interface ManagerDirectoryViewModel {
  id: string;
  name: string | null;
  icon: string | null;
  color_index: number;
}
