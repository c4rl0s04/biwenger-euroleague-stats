# =============================================================================
# BIWENGER STATS - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# 1. DATABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Docker: Use 'postgres' | Local: Use 'localhost'
POSTGRES_HOST=localhost
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=biwenger
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# 2. BIWENGER CREDENTIALS (Required for Sync)
# -----------------------------------------------------------------------------
# Bearer Token: Get from browser Network tab (Authorization header)
BIWENGER_TOKEN=your_jwt_token_here
# League ID: Get from URL or X-League header
BIWENGER_LEAGUE_ID=your_league_id
# User ID: Get from User Profile or X-User header
BIWENGER_USER_ID=your_user_id

# -----------------------------------------------------------------------------
# 3. AUTHENTICATION (Required for Login)
# -----------------------------------------------------------------------------
# Password to access the dashboard
ACCESS_PASSWORD="your_secure_password"
# Random string for session encryption (generate with `openssl rand -base64 32`)
AUTH_SECRET="your_random_secret_string"

# -----------------------------------------------------------------------------
# 4. EXTERNAL SERVICES (Optional)
# -----------------------------------------------------------------------------
# Google Gemini API Key for AI features
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_key

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - For Docker: Variables in docker-compose.yml override default .env
# - For Local Dev: .env.local overrides everything
# - Token Expiration: If sync fails (401), get a fresh BIWENGER_TOKEN

